# Tasks: task-p0-1-safe-command-runner

- [x] 1. Add `run_cmd_args` and `run_cmd_legacy` helpers in `lib/logging.sh`.
- [x] 2. Replace direct `eval` usage in `run_cmd` with new helpers.
- [x] 3. Ensure dry-run and logging behavior remains intact.
- [x] 4. Run smoke validation commands.
- [x] 5. Update OpenSpec artifacts if implementation deviates.
