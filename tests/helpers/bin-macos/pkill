#!/usr/bin/env bash
# Stub for macOS-only `pkill`: records argv, never executes.
if [ -n "${MDOCTOR_STUB_LOG:-}" ]; then
  printf "pkill %s\n" "$*" >>"$MDOCTOR_STUB_LOG"
fi
echo "stubbed pkill: $*" >&2
exit 0
