#!/usr/bin/env bash
# Stub for macOS-only `ipconfig`: records argv, never executes.
if [ -n "${MDOCTOR_STUB_LOG:-}" ]; then
  printf "ipconfig %s\n" "$*" >>"$MDOCTOR_STUB_LOG"
fi
echo "stubbed ipconfig: $*" >&2
exit 0
